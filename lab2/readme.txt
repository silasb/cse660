This is the repo for my cse660 class

=== Compile ===
First you need to cd into lab2
  cd lab2

Then you need to create a bin directory
  mkdir bin

The last step to compile is to run `make`
  make

The binary will be called `lab2` in `lab2/bin`

Now just run:
  bin/lab2 2 2 2
